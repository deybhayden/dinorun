{
  "id": "ffcc4d8f",
  "title": "Initialize dinorun Phaser 4 project",
  "tags": [
    "dinorun",
    "mvp"
  ],
  "status": "closed",
  "created_at": "2026-05-05T19:41:54.042Z"
}

Set up a Vite + TypeScript + Phaser 4 project in `dinorun/` with npm scripts for dev/build, a basic HTML entrypoint, and a `src/main.ts` game bootstrap.

Completed:
- Added `package.json` with `dev`, `build`, and `preview` scripts.
- Installed Phaser 4, Vite, and TypeScript; generated `package-lock.json`.
- Added `index.html`, `vite.config.ts`, `tsconfig.json`, `.gitignore`.
- Added `src/main.ts` Phaser bootstrap with a basic BootScene.
- Added `src/styles.css` for full-window canvas layout.
- Verified with `npm run build`.
